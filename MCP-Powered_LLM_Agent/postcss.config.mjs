// Tailwind CSS v4 uses a dedicated PostCSS plugin (@tailwindcss/postcss)
// instead of the old "tailwindcss" plugin. No tailwind.config.ts needed.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
